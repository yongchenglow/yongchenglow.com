if ! command -v gitleaks >/dev/null 2>&1; then
	echo "gitleaks not installed: brew install gitleaks (bypass once with --no-verify)"
	exit 1
fi
gitleaks git --pre-commit --staged --redact --no-banner
lint-staged
