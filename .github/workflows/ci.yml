name: CI/CD

on:
  push:
    branches: [main]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  get-node-version:
    name: Get Node Version
    runs-on: ubuntu-latest
    outputs:
      node_version: ${{ steps.get-version.outputs.version }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Get Node version from package.json
        id: get-version
        run: echo "version=$(node -p "require('./package.json').engines.node")" >> $GITHUB_OUTPUT

  build:
    name: Build & Test
    permissions:
      contents: read
      security-events: write
    needs: get-node-version
    uses: ./.github/workflows/reusable-build.yml
    with:
      node_version: ${{ needs.get-node-version.outputs.node_version }}

  docker:
    name: Build & Push Docker Image
    needs: build
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/reusable-docker.yml
    with:
      platforms: linux/amd64,linux/arm64
    secrets: inherit

  security-scan:
    name: Security Scan
    needs: docker
    permissions:
      contents: read
      packages: read
      security-events: write
    uses: ./.github/workflows/reusable-security-scan.yml
    with:
      image_tag: ${{ needs.docker.outputs.image_tag }}
    secrets: inherit

  deploy-production:
    name: Deploy to Production
    needs: [docker, security-scan]
    permissions:
      contents: read
      packages: read
      deployments: write
    uses: ./.github/workflows/production-deploy.yml
    with:
      image_tag: ${{ needs.docker.outputs.image_tag }}
    secrets: inherit
