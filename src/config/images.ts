// Source-image budget for files in public/img. The widest slot on the site is
// the blog image modal (max-w-5xl, 1024 CSS px), so 2048px covers 2x displays.
export const IMAGE_DIR = "public/img";
export const MAX_IMAGE_DIMENSION = 2048;
export const MAX_IMAGE_BYTES = 400 * 1024;
export const IMAGE_QUALITY = 80;
